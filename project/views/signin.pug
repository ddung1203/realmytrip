extends layout

block content
  .container
    .clearfix
      form.form-signin(action='/signin', method='POST')
        h3.form-signin-heading 로그인
        label.sr-only(for='email') Email address
        p
        input(
          type='email', id='email', name='email', class='form-control', placeholder='이메일', required, autofocus)
        label.sr-only(for='password') Password
        input(
          type='password', id='password', name='password', class='form-control', placeholder='비밀번호', required, autofocus)
        button.btn.btn-primary.btn-block(type='submit') 로그인
        a.btn.btn-dark.btn-block(href='/auth/facebook')
          i.fa.fa-facebook.fa-lg
          | &nbsp;
          | 페이스북으로 로그인